# sacarDependencias
```
usage: sacarDependencias.py [-h] [-f FILE] [-d DIRECTORY]

optional arguments:
  -h, --help            show this help message and exit
  -f FILE, --file FILE  Fichero a depurar
  -d DIRECTORY, --directory DIRECTORY
                        Directorio a depurar
```
